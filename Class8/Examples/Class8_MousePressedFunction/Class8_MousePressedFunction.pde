void setup() {
}

void draw() {
}

// call on keyPressed once.
void mousePressed() {
  int r = int(random(0, 255));
  int g = int(random(0, 255));
  int b = int(random(0, 255));
  background(r,g,b);
}