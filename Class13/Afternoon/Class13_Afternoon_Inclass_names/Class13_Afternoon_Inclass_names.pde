// create an array of Strings
String[] studentNames = {"Frederick", "Fred", "Sam", "Diana"};

// loop through and print every name.
for(int i = 0;i < studentNames.length;i++){
  println(studentNames[i]);
}